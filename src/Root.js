import React, { Component } from 'react';
import PropTypes from 'prop-types';

class Root extends Component {
  render() {
    return (
      <h1>
        webpack first
      </h1>
    );
  }
}

Root.propTypes = {

};

export default Root;